[global]
    # Notification placement
    monitor = 0
    origin = top-right
    offset = "25x125"
    width = (0,400)
    height = 100

    # Font settings
    font = IntelOneMono Bold 16
    alignment = center
    max_icon_size = 32
    icon_path = /home/ksnyder/.local/share/icons/

    # Color scheme
    background = "#282828"
    foreground = "#d79921"

    # Frame and separator settings
    frame_width = 2
    separator_height = 2

    # Padding inside the notification
    padding = 10

    # Progress bar
    progress_bar = true
    progress_bar_height = 10
    progress_bar_frame_width = 1
    progress_bar_min_width = 150
    progress_bar_max_width = 400
    progress_bar_corner_radius = 5

    # History
    history_length = 20
    mouse_left_click = do_action, close_current
    mouse_middle_click = do_action, close_current
    mouse_right_click = do_action, close_all

    # Timeout
    timeout = 10
    
[urgency_low]
    background = "#282828"
    foreground = "#d79921"
    timeout = 10

[urgency_normal]
    background = "#282828"
    foreground = "#d79921"
    timeout = 10

[urgency_critical]
    background = "#cc241d"
    foreground = "#282828"
    timeout = 0

[firefox_download]
    appname = Firefox
    summary = "*download*"
    urgency = low
